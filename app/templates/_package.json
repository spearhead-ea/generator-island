{
  "name": "<%= app_name %>-island",
  "version": "1.0.0",
  "private": true,
  "main": "dist/app.js",
  "scripts": {
    "clean": "gulp clean",
    "build": "gulp build",
    "start": "gulp start",
    "debug": "gulp build && gulp debug"
  }
}
